<%= render "search_form", airport_options: @airport_options,
    flights_dates: @flights_dates %>

<% if params[:departure_airport].present? %>
    <%= render "flights_found", flights: @flights %>
<% end %>